#include <stdio.h>
#define Binh '-'

int main ()
{
 char c , lastc ;
 lastc = Binh;
 while ((c = getchar ())) != EOF )
 {
     if ( c == ' ') 
     {
         if(lastc == ' ') putchar (c);
     }
     else putchar(c);
     lastc = c;
 }

 return;
}
